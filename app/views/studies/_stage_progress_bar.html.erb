<ul>
  <% timeline.each do |stage, entry| %>
    <%= render partial: "studies/stage_progress_bar_entry",
               locals: { label: entry[:label], state: entry[:state], stage: stage } %>
  <% end %>
</ul>