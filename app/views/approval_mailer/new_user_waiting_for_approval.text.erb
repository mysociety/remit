<% message.subject = "New user waiting for approval on ReMIT: #{@user.email}" %>
There's a new user who needs your approval on ReMIT.

<%= @user.name %>
<%= @user.email %>

You can approve them here:

<%= edit_admin_user_url(@user) %>