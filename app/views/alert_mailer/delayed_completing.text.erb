<% message.subject = "Study #{@study.reference_number} (#{truncate(@study.title)}) is delayed completing" %>
Dear <%= @user.name %>

This is an automated alert to let you know that:

<%= @study.title %>

Reference number: <%= @study.reference_number %> is delayed completing according to the
information we have in the ReMIT database.

The expected completion date was <%= @study.expected_completion_date.to_formatted_s(:short_ordinal) %>.

You can see this study on ReMIT here:

<%= study_url(@study) %>