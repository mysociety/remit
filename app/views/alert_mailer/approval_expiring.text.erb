<% message.subject = "Study #{@study.reference_number} (#{truncate(@study.title)}) ERB approval is expiring" %>
Dear <%= @user.name %>

This is an automated alert to let you know that:

<%= @study.title %>

Reference number: <%= @study.reference_number %> is approaching its ERB approval expiry date according to the information we have in the MSF OCA Research Management and Impact Tool (ReMIT).

The ERB approval expiry date is <%= @study.erb_approval_expiry.to_formatted_s(:medium_ordinal) %>.

You can see this study on ReMIT here:

<%= study_url(@study) %>

Many thanks,

MSF ReMIT team