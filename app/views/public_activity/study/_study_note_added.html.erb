<% content_for :extra_classes, flush: true do %> <% end %>
<% content_for :title, flush: true do %>
    Note left<%= " by #{a.owner.name}" unless a.owner.blank? %>
<% end %>
<% content_for :description, flush: true do %>
    <blockquote class="timeline__item__note">
        <%= simple_format(a.related_content.notes) %>
    </blockquote>
<% end %>